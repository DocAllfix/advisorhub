/**
 * Messaggio unico per ogni blocco della versione dimostrativa. Vive in un
 * modulo senza dipendenze server, così può essere importato sia dalle server
 * action (via auth-helpers) sia dai componenti client, senza duplicare il testo.
 */
export const MESSAGGIO_DEMO = "Non disponibile nella versione dimostrativa.";
